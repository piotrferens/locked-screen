import { PinVerification } from '@/features/pin-verification/pin-verification';

const Home = () => (
  <main className="flex flex-col p-6 gap-6 mt-6">
    <PinVerification />
  </main>
);

export default Home;
